// SensorData class, contains same fields as the json received from the backend server containing the sensor data. 

export class SensorData {
    Time: [String];
    Temperature: number;
    Humidity: number;
  }